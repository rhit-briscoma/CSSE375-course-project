package domain.checks.style_checks;

import domain.checks.Check;

// this class is an abstraction of Style Checks and allows for flexibility in adding new style checks

public abstract class StyleCheck extends Check {

    // protected MyClassNode node = null;
    
}
