package domain.checks.design_pattern_checks;

import domain.checks.Check;

// this class is an abstraction of Pattern Checks and allows for flexibility in adding new pattern checks

public abstract class PatternCheck extends Check {
    // protected MyClassNode node;
}
