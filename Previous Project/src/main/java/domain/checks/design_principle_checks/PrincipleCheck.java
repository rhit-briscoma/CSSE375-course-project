package domain.checks.design_principle_checks;

import domain.checks.Check;

// this class is an abstraction of Principle Checks and allows for flexibility in adding new principle checks
public abstract class PrincipleCheck extends Check {

    // protected ArrayList<MyClassNode> nodes;
    // protected MyClassNode node;


    
}
